version: "3.3"

services:
  nginx:
    restart: always
    build: .
    volumes:
      - /var/www/ThalosCinemaSystem/staticfiles:/var/www/ThalosCinemaSystem/staticfiles
      - /var/www/ThalosCinemaSystem/mediafiles/upload_thumbnails:/var/www/ThalosCinemaSystem/mediafiles/upload_thumbnails
    ports:
      - "80:80"
    networks:
      - nginx_network

networks:
  nginx_network:
    external: true